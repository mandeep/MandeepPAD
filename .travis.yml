language: python
python:
  - "3.5"
notifications:
  email: false
before_install:
  - pip install coveralls
install:
  - python setup.py install
  - pip install coverage
  - pip install pytest
  - pip install pyqt5
script:
  - py.test
  - coverage run test_mpad.py
after_success:
  - coveralls